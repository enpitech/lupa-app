/**
 * Re-exports and type aliases for Pintura editor integration.
 *
 * We import from the library's own typings so our app stays in sync
 * with the actual component props and callback signatures.
 */
export type { PinturaImageState, PinturaReadState } from '@pqina/pintura';

export type { default as PinturaEditorComponent } from '@pqina/react-native-expo-pintura';
